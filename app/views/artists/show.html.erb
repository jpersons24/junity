<h1><%= @artist.name %></h1>

<div>
   <p>About:</p>
   <ul>
      <li>Age: <%= @artist.age %></li>
      <li>Bio: <%= @artist.bio %> </li>
   </ul>
</div>

<p><%= link_to 'Edit Artist Information', edit_artist_path(@artist) %></p>

<%= link_to 'Add artist to your favorites list!', new_favorite_artist_path %>