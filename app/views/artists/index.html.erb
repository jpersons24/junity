<h1>All Artists</h1>

<p><%= link_to "Click here to add artist!", new_artist_path %></p>

<ul>
   <% @artists.each do |artist| %>
      <li><%= link_to artist.name, artist_path(artist) %></li>
   <% end %>
</ul>